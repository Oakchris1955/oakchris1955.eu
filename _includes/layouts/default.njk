---
title: Oakchris1955's corner on the Internet
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" type="image/bmp" href="/favicon.ico">
    <link rel="stylesheet" href="/css/theme.css">
    <title>{{ title }}</title>
  </head>
  <body>
    <header>
      <nav>
        <ul class="nav">
          {% for link in header_links %}
            <li class="nav-item"><a href="{{ link.url }}"{% if link.url == page.url %} aria-current="page"{% endif %}>{{ link.title }}</a></li>
          {% endfor %}
        </ul>
      </nav>
    </header>
    <hr>
    <main>
      {{ content | safe }}
    </main>
    <hr>
    <footer>
      <nav style="font-size: smaller" class="plain-links">
        <ul class="nav">
          {% for link in footer_links %}
            <li class="nav-item"><a href="{{ link.url }}">{{ link.title }}</a></li>
          {% endfor %}
        </ul>
      </nav>
      &copy; 2025 Oakchris1955
    </footer>
  </body>
</html>
