---
title: Oakchris1955's corner on the Internet
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta property="og:title" content="{{ title }}"/>
    <meta property="og:type" content="website"/>
    <meta property="og:image" content="https://oakchris1955.eu/imgs/avatar.png"/>
    {% if description %}
    <meta property="og:description" content="{{ description }}">
    {% endif %}
    <link rel="shortcut icon" type="image/bmp" href="/favicon.ico">
    <link rel="stylesheet" href="/css/theme.css">
    <title>{{ title }}</title>
  </head>
  <body>
    <header>
      <nav>
        <ul class="nav">
          {% for link in header_links %}
            <li class="nav-item"><a href="{{ link.url }}"{% if link.url == page.url %} aria-current="page"{% endif %} {%if link.blank %} target="_blank"{% endif %}>{{ link.title }}</a></li>
          {% endfor %}
        </ul>
      </nav>
    </header>
    <hr>
    <main>
      {{ content | safe }}
    </main>
    <hr>
    <footer>
      <nav style="font-size: smaller" class="plain-links">
        <ul class="nav">
          {% for link in footer_links %}
            <li class="nav-item"><a href="{{ link.url }}" {%if link.blank %} target="_blank"{% endif %}>{{ link.title }}</a></li>
          {% endfor %}
        </ul>
      </nav>
      &copy; 2025 Oakchris1955
    </footer>
  </body>
</html>
